# Used with: docker compose -f docker-compose.trackoneagent.yml --env-file agent.docker.env up -d
# Copy to agent.docker.env in the repo root (or pass -e to docker run).

TRACKONE_CONTROL_URL=http://192.168.1.50:8000
TRACKONE_API_TOKEN=paste_token_from_build_trackoneagent_bundle_or_create_monitored_host

# Optional
TRACKONE_INTERVAL_SECONDS=30
